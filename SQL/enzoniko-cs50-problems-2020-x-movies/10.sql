SELECT name FROM people WHERE people.id in (SELECT DISTINCT  directors.person_id FROM directors INNER JOIN movies ON directors.movie_id = movies.id INNER JOIN ratings ON ratings.movie_id = movies.id WHERE rating >= 9.0);